<idea-plugin>
    <id>io.github.unurgunite.crystal</id>
    <name>Fast Crystal Plugin</name>
    <vendor url="https://github.com/unurgunite">unurgunite</vendor>
    <description><![CDATA[
<p>
  Comprehensive Crystal language support for all JetBrains IDEs — IntelliJ IDEA, RubyMine, WebStorm, CLion, and others.
  Provides a rich development experience with syntax highlighting, code intelligence, debugging, and an integrated test runner,
  all powered by a native GrammarKit parser (no external language server required).
</p>

<p>
  <b>Early Beta</b> — This plugin is under active development and bugs are to be expected.
  If you encounter a problem, please <a href="https://github.com/unurgunite/intellij-crystal/issues/new/choose">open a GitHub issue</a>
  with a minimal reproduction — that helps us fix it faster than a negative review would.
</p>

<h2>Debugging</h2>
<p>
  Full debugger integration via <b>LLDB DAP</b> (Debug Adapter Protocol). Set breakpoints, step through code,
  inspect variables, and evaluate expressions — for both programs and specs.
  Requires <code>lldb-dap</code> to be installed on your system.
</p>
<ul>
  <li>Breakpoints with hit counts and conditions</li>
  <li>Step over / into / out</li>
  <li>Variable inspection (locals, instance variables, globals)</li>
  <li>Debug both <code>crystal run</code> and <code>crystal spec</code> targets</li>
  <li>Crystal-specific LLDB type formatters bundled</li>
</ul>

<h2>Test Runner</h2>
<p>
  Integrated test runner connecting <code>crystal spec</code> to IntelliJ's Test UI (SMTRunner).
  Run individual specs from gutter icons, see real-time results, and re-run failed tests instantly.
</p>
<ul>
  <li>Gutter run icons on <code>describe</code> and <code>it</code> blocks</li>
  <li>Single-test execution via <code>file:line</code> targeting</li>
  <li>Real-time output parsing with pass/fail/error/pending states</li>
  <li>Re-run failed tests with one click</li>
  <li>Spec file icons for visual distinction</li>
</ul>

<h2>Code Intelligence</h2>
<ul>
  <li><b>Go to Definition</b> (Ctrl+B / Ctrl+Click) — jump to classes, modules, structs, enums, method definitions, and DOT-call methods (<code>obj.method</code>, <code>Class.method</code>) across the entire project</li>
  <li><b>Namespace Access</b> — Go to Definition and hover for intermediate namespace segments (e.g. <code>Inner</code> in <code>Outer::Inner.method</code>)</li>
  <li><b>Code Completion</b> (Ctrl+Space) — context-aware dot-completion with type inference, free-text completion for classes/methods/locals, type completion in annotations and generics, overloaded methods as separate entries, record macro support, and <code>::</code> auto-completion</li>
  <li><b>Parameter Info</b> (Ctrl+P) — method signatures at call sites, supports parenthesized calls, bare calls, DOT-calls, <code>ClassName.new(...)</code>, and overloads</li>
  <li><b>Hover Type Info</b> — hovering over a variable shows the inferred type; method return types inferred from body when no annotation exists</li>
  <li><b>Quick Documentation</b> (Ctrl+Q) — rendered doc comments with syntax-highlighted signature, Markdown support, and clickable type names</li>
  <li><b>Find Usages</b> (Alt+F7) — locate all references to a symbol across the project</li>
  <li><b>Go to Symbol / Class</b> (Ctrl+N, Ctrl+Alt+Shift+N) — project-wide instant navigation via StubIndex</li>
  <li><b>Structure View</b> — PSI-based tree showing classes, modules, methods, macros, and nested definitions</li>
</ul>

<h2>Code Quality</h2>
<ul>
  <li><b>Type Checking</b> — validates argument types against parameter annotations (supports numeric autocasting, union types, nilable types, overloads)</li>
  <li><b>Argument Count Validation</b> — reports missing required arguments and excess arguments (supports named args, splat, double-splat, default values)</li>
  <li><b>Unused Variable Detection</b> — warns on local variables that are assigned but never read</li>
  <li><b>Lib Fun Type Annotation</b> — reports missing type annotations in lib function definitions</li>
  <li><b>Empty Collection Inspection</b> — suggests using typed empty literals</li>
  <li><b>Colon Spacing</b> — reports missing space after <code>:</code> in type annotations</li>
  <li><b>Instance Variable Type</b> — validates instance variable types against declarations</li>
  <li><b>Invalid Single-Quote String</b> — reports non-ASCII characters in single-quote strings</li>
</ul>

<h2>Editor Features</h2>
<ul>
  <li><b>Syntax Highlighting</b> — 60+ keywords, operators, strings with interpolation, regex, symbols, heredocs, percent literals, annotations, macros</li>
  <li><b>Semantic Highlighting</b> — PSI-based annotator distinguishes variables, methods, types, parameters, and macro fresh vars</li>
  <li><b>Keyword Block Highlighting</b> — cursor on block keywords highlights all related structural keywords of the enclosing block</li>
  <li><b>Code Formatting</b> — reformat via <code>crystal tool format</code> (Ctrl+Alt+L)</li>
  <li><b>Rename Refactoring</b> (Shift+F6) — in-place rename with preview dialog and automatic compiler verification</li>
  <li><b>Live Templates</b> — 21 code snippets for common Crystal patterns (def, class, module, spec, etc.)</li>
  <li><b>Smart Enter</b> — automatically inserts <code>end</code> after def/class/module/if/do and handles indentation</li>
  <li><b>Code Folding</b> — collapse methods, classes, blocks, comments, heredocs, arrays, and hashes</li>
  <li><b>Brace Matching</b> — highlights matching pairs for parentheses, brackets, braces, and do/end</li>
  <li><b>TODO Indexing</b> — Crystal TODO/FIXME comments appear in the TODO tool window</li>
</ul>

<h2>Run Configurations</h2>
<ul>
  <li><b>Crystal Run</b> — run Crystal programs with configurable arguments and environment</li>
  <li><b>Crystal Build</b> — compile with custom flags (release, static, target, etc.)</li>
  <li><b>Crystal Spec</b> — run specs with file/line targeting and tag filters</li>
</ul>

<h2>Parser Coverage</h2>
<p>
  The plugin ships a full GrammarKit BNF parser covering classes, modules, structs, enums, methods, macros,
  control flow, generics (variadic, defaults, bounds), union types, blocks, proc/lambda literals, pattern matching,
  lib blocks (fun, union, struct, enum, external vars, varargs), annotations, heredocs, percent literals,
  string interpolation in percent/regex/backtick literals, implicit object bracket access (<code>&amp;.[]</code>),
  rescue clauses with typed rescue and union types, ranges with omitted start, namespace access (<code>::Foo</code>),
  and more. The parser is error-tolerant and works with incomplete code while typing.
</p>

<h2>Requirements</h2>
<ul>
  <li><b>Crystal</b> installed and available in PATH (for formatting, compiler verification, and running programs)</li>
  <li><b>lldb-dap</b> (optional) — required for debugging. Install via your system package manager.</li>
</ul>
]]></description>

    <change-notes><![CDATA[
<h3>0.1.17</h3>
<ul>
  <li><b>Keyword block highlighting</b> — cursor on structural keywords highlights all related keywords of the enclosing block</li>
  <li><b>Variable hover type info</b> — hovering over a variable shows the inferred type in a two-line popup</li>
  <li><b>Method return type inference</b> — methods without explicit return type annotations have their return type inferred from body</li>
  <li><b>Hash/tuple type inference</b> — detailed type parameters in hover popups for hash and tuple literals</li>
  <li><b>Ternary type inference</b> — ternary expressions correctly infer union types for variable hover</li>
  <li><b>Namespace access Go to Definition and hover</b> — hover and navigate on intermediate namespace segments</li>
  <li><b>DOT-call Go to Definition and hover</b> — <code>obj.method</code> resolves to target method; hovering shows documentation</li>
  <li><b>Parameter/Definition hover popups</b> — parameter names and definition names show context-specific popups</li>
  <li><b>Overloaded methods in completion</b> — multiple overloads appear as separate entries with signatures</li>
  <li><b><code>::</code> auto-completion</b> — typing <code>::</code> after a CONSTANT triggers completion popup</li>
  <li><b>Colon spacing inspection</b> — reports missing space after <code>:</code> in type annotations</li>
  <li><b>Instance variable type inspection</b> — validates instance variable types</li>
  <li><b>String interpolation in percent/regex/backtick literals</b> — <code>%(hello #{name})</code>, <code>/pattern #{expr}/</code>, <code>`echo #{name}`</code> now parse correctly</li>
  <li><b>Rescue clause improvements</b> — typed rescue with union types and variable binding</li>
  <li><b>Implicit object bracket access</b> — <code>&amp;.[1]</code>, <code>&amp;[]</code> now parse correctly</li>
  <li><b>Bug fix</b> — Go to Definition no longer freezes on bare method calls</li>
  <li><b>Bug fix</b> — false positive type mismatches on DOT-calls fixed</li>
  <li><b>Bug fix</b> — false "Value assigned never used" with conditional reassignment fixed</li>
</ul>

<h3>0.1.16</h3>
<ul>
  <li><b>Colon spacing inspection</b> — reports missing space after <code>:</code> in type annotations (e.g. <code>x:Int32</code> → warning)</li>
  <li><b><code>@param</code> shorthand support</b> — <code>@param</code> in method bodies is recognized for completion, type inference, and inspections</li>
  <li><b>Record macro support</b> — <code>record Config, host : String, port : Int32</code> works for completion, parameter info (<code>Ctrl+P</code>), and argument inspections</li>
  <li><b>Type checking for record macro parameters</b> — validates argument types against record parameters in inspections</li>
  <li><b>Parameter completion priority boost</b> — parameters appear higher in the completion popup with bold styling</li>
  <li><b>Keywords as method/macro names</b> — <code>macro require(...)</code>, <code>def if(...)</code>, <code>def self.require(...)</code> etc. now parse correctly; all Crystal keywords are valid as method names</li>
  <li><b>Annotation definitions inside module/class bodies</b> — <code>annotation GeneratedWrapper ... end</code> inside <code>module</code>/<code>class</code> now parses correctly</li>
  <li><b>Global namespace prefix in expressions</b> — <code>::Bytes.new(...)</code>, <code>::Foo::Bar.new(x)</code> etc. now parse correctly</li>
  <li><b>Overloaded methods in completion</b> — multiple overloads of the same method (e.g. <code>ENV.fetch</code> with 3 different signatures) now appear as separate entries in the code completion popup, each showing its parameter signature</li>
  <li><b>Bug fix</b> — renaming <code>@var</code>/<code>@@var</code> with explicit prefix now correctly preserves the variable type</li>
  <li><b>Bug fix</b> — postfix modifiers (e.g. <code>return x if condition</code>) no longer break macro body <code>end</code> detection</li>
  <li><b>Bug fix</b> — type check and parameter info for DOT-calls on constants no longer show false positive type mismatches</li>
  <li><b>Bug fix</b> — false positive "Unknown named argument" on class constructor DOT-calls fixed</li>
  <li><b>Bug fix</b> — <code>case ... end.tap do</code> no longer causes a parse error</li>
  <li><b>Bug fix</b> — <code>?</code>/<code>!</code> suffix after macro interpolation now parses correctly</li>
  <li><b>Bug fix</b> — multi-level pointer types in lib bindings (e.g. <code>BaseInfo***</code>) now parse correctly</li>
  <li><b>Bug fix</b> — postfix modifier after compound assignment (e.g. <code>@data[key] += 1 if condition</code>) now parses correctly</li>
  <li><b>Bug fix</b> — binary expressions inside calls are no longer misparsed as bare method calls</li>
  <li><b>Bug fix</b> — splat prefix detection in argument extraction no longer produces false positives</li>
  <li><b>Bug fix</b> — record macro parameter priority fixed, preventing false "wrong argument type" errors</li>
  <li><b>Bug fix</b> — colon spacing inspection now only scans within method definitions</li>
  <li><b>Bug fix</b> — binary <code>+</code>/<code>-</code> no longer parsed as bare method arguments</li>
  <li><b>Bug fix</b> — Go to Definition on <code>.new</code> now follows Crystal's constructor resolution order</li>
  <li><b>Bug fix</b> — operator method names (<code>[]</code>, <code>[]?</code>, <code>[]=</code>) now parse correctly</li>
  <li><b>Bug fix</b> — <code>ENV.fetch</code> completion fixed; all stdlib methods inside <code>module ENV</code> are now indexed</li>
  <li><b>Changed</b> — method lookup elements use PSI object identity to distinguish overloaded methods</li>
  <li><b>Changed</b> — Force Re-index button now properly removes the library before re-adding it</li>
  <li><b>Changed</b> — Force Re-index shows background progress bar with status updates</li>
</ul>

<h3>1.1.15</h3>
<ul>
  <li><b>Parameter info (Ctrl+P)</b> for <code>ClassName.new</code> — now shows <code>initialize</code> method parameters when calling <code>new</code> on a class</li>
  <li><b>Type check for <code>ClassName.new</code></b> — argument type validation now works for <code>new</code> calls, resolving to <code>initialize</code> parameters</li>
  <li><b>Bug fix</b> — <code>findTypeByName</code> now prefers the class from the current file when multiple classes with the same name exist in the project</li>
</ul>

<h3>1.1.13</h3>
<ul>
  <li><b>Code folding</b> — multi-line arrays (<code>[...]</code>) and hashes (<code>{...}</code>) can now be collapsed, matching RubyMine behavior</li>
  <li><b>Code folding</b> — all block constructs keep keyword and signature visible when collapsed; placeholder shows <code> ... end</code> (or <code>[ ... ]</code> / <code>{ ... }</code> for arrays/hashes), matching RubyMine behavior</li>
  <li>Supported keywords: <code>if</code>/<code>unless</code>/<code>while</code>/<code>until</code> (with condition), <code>def</code> (with name + params), <code>class</code>/<code>module</code>/<code>struct</code>/<code>enum</code> (with name), <code>begin</code>, <code>do</code>, <code>case</code>/<code>for</code>, <code>macro</code>, <code>lib</code>, <code>annotation</code>, <code>verbatim do</code></li>
  <li>Suppress code completion after numeric literals — typing a number (e.g. <code>a = 1</code>) no longer triggers variable/method suggestions, matching RubyMine behavior</li>
  <li><b>Bug fix</b> — nil-safe index (<code>?</code>) no longer breaks parser in block contexts; <code>item["states"]?</code> inside <code>each do</code>/<code>if</code> blocks no longer causes false parse errors</li>
</ul>

<h3>0.1.12</h3>
<ul>
  <li>Raised minimum IDE version from 2025.1 to 2026.1 (<code>sinceBuild</code> 251 → 261) to match the target platform</li>
</ul>

<h3>0.1.11</h3>
<ul>
  <li>Improved debugger compatibility on Windows — added Windows-specific lldb-dap binary discovery, .exe suffix for compiled binaries, and fixed formatter path handling for Windows</li>
</ul>

<h3>0.1.9</h3>
<ul>
  <li>Patched <code>crystal_formatters.py</code> to run on Windows — debugger now works on Windows as well</li>
</ul>

<h3>0.1.7</h3>
<ul>
  <li>Initial early-beta release (Proof of Concept)</li>
  <li><b>Parser</b> — GrammarKit BNF covering classes, modules, methods, macros, generics, control flow, pattern matching, lib bindings, proc literals, annotations, and more</li>
  <li><b>Syntax Highlighting</b> — 60+ keywords, operators, strings, interpolation, heredocs, percent and command literals, symbols, regex</li>
  <li><b>Debugger</b> — LLDB/DAP integration with breakpoints, stepping, variable inspection; works for programs and specs</li>
  <li><b>Test Runner</b> — integrated Crystal spec runner (SMTRunner) with real-time output, gutter icon single-test execution, folder-level running, re-run failed</li>
  <li><b>Navigation</b> — Go to Definition, Go to Symbol/Class, Find Usages, Parameter Info, Structure View</li>
  <li><b>Code Completion</b> — context-aware dot-completion with type inference, free-text, type annotation completion</li>
  <li><b>Inspections</b> — type checking, argument count validation, unused variable detection, lib fun type annotations</li>
  <li><b>Run Configurations</b> — crystal run, build, and spec with environment and argument support</li>
  <li><b>Refactoring</b> — rename with preview dialog and compiler verification</li>
  <li><b>Quick Documentation</b> — rendered doc comments with Markdown</li>
  <li><b>Smart Enter</b> — auto-insert <code>end</code> with correct indentation</li>
  <li><b>Code Formatting</b> — delegates to <code>crystal tool format</code></li>
  <li><b>21 Live Templates</b> — code snippets for common Crystal patterns</li>
  <li><b>Semantic Highlighting</b> — PSI annotator distinguishing variables, methods, types, parameters</li>
</ul>
]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <incompatible-with>net.kenro.ji.jin.intellij.crystal-2</incompatible-with>

    <dependencies>
        <module name="intellij.platform.dap"/>
    </dependencies>

    <extensions defaultExtensionNs="com.intellij">
        <fileType
                name="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalFileType"
                fieldName="INSTANCE"
                language="Crystal"
                extensions="cr"
                patterns="*.cr"/>

        <lang.parserDefinition
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalParserDefinition"/>

        <lang.syntaxHighlighterFactory
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.highlighting.CrystalSyntaxHighlighterFactory"/>

        <lang.commenter
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalCommenter"/>

        <lang.braceMatcher
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalBraceMatcher"/>

        <codeBlockSupportHandler
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalCodeBlockSupportHandler"/>

        <lang.foldingBuilder
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalFoldingBuilder"/>

        <lang.documentationProvider
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.documentation.CrystalDocumentationProvider"/>

        <colorSettingsPage
                implementation="io.github.unurgunite.crystal.highlighting.CrystalColorSettingsPage"/>

        <lang.quoteHandler
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalQuoteHandler"/>

        <typedHandler
                implementation="io.github.unurgunite.crystal.CrystalTypedHandler"/>

        <enterHandlerDelegate
                implementation="io.github.unurgunite.crystal.CrystalEnterHandler"/>

        <annotator
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.highlighting.CrystalAnnotator"/>

        <highlightErrorFilter
                implementation="io.github.unurgunite.crystal.highlighting.CrystalHighlightErrorFilter"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Type mismatch in method arguments"
                shortName="CrystalTypeMismatch"
                enabledByDefault="true"
                level="ERROR"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalTypeCheckInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Wrong number of arguments in method call"
                shortName="CrystalArgumentCount"
                enabledByDefault="true"
                level="WARNING"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalArgumentCountInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Unused variable"
                shortName="CrystalUnusedVariable"
                enabledByDefault="true"
                level="WEAK WARNING"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalUnusedVariableInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Empty collection literal without type annotation"
                shortName="CrystalEmptyCollection"
                enabledByDefault="true"
                level="ERROR"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalEmptyCollectionInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Missing type annotation in lib fun parameter"
                shortName="CrystalLibFunParameterType"
                enabledByDefault="true"
                level="ERROR"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalLibFunParameterTypeInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Invalid single-quote string"
                shortName="CrystalSingleQuoteString"
                enabledByDefault="true"
                level="ERROR"
                implementationClass="io.github.unurgunite.crystal.highlighting.CrystalSingleQuoteStringInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Colon spacing in type annotations"
                shortName="CrystalColonSpacing"
                enabledByDefault="true"
                level="ERROR"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalColonSpacingInspection"/>

        <localInspection
                language="Crystal"
                groupName="Crystal"
                displayName="Invalid instance variable type"
                shortName="CrystalInstanceVarType"
                enabledByDefault="true"
                level="ERROR"
                implementationClass="io.github.unurgunite.crystal.inspections.CrystalInstanceVarTypeInspection"/>

        <todoIndexer
                filetype="Crystal"
                implementationClass="io.github.unurgunite.crystal.CrystalTodoIndexer"/>

        <lang.psiStructureViewFactory
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.structure.CrystalStructureViewFactory"/>

        <configurationType
                implementation="io.github.unurgunite.crystal.run.CrystalRunConfigurationType"/>

        <runConfigurationProducer
                implementation="io.github.unurgunite.crystal.run.CrystalRunConfigurationProducer"/>

        <runLineMarkerContributor
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.run.CrystalRunLineMarkerProvider"/>

        <testLocator
                implementation="io.github.unurgunite.crystal.run.CrystalTestLocator"/>

        <defaultLiveTemplates file="/liveTemplates/Crystal.xml"/>

        <liveTemplateContext
                contextId="CRYSTAL"
                implementation="io.github.unurgunite.crystal.CrystalTemplateContextType"/>

        <formattingService
                implementation="io.github.unurgunite.crystal.formatting.CrystalFormattingService"/>

        <gotoSymbolContributor
                implementation="io.github.unurgunite.crystal.navigation.CrystalGoToSymbolContributor"/>

        <gotoDeclarationHandler
                implementation="io.github.unurgunite.crystal.navigation.CrystalGotoDeclarationHandler"/>

        <lang.findUsagesProvider
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.navigation.CrystalFindUsagesProvider"/>

        <highlightUsagesHandlerFactory
                implementation="io.github.unurgunite.crystal.highlighting.CrystalHighlightUsagesHandlerFactory"/>

        <findUsagesHandlerFactory
                implementation="io.github.unurgunite.crystal.navigation.CrystalFindUsagesHandlerFactory"/>

        <referencesSearch
                implementation="io.github.unurgunite.crystal.navigation.CrystalInstanceVarReferencesSearcher"/>

        <gotoClassContributor
                implementation="io.github.unurgunite.crystal.navigation.CrystalGoToClassContributor"/>

        <lang.refactoringSupport
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.refactoring.CrystalRefactoringSupportProvider"/>

        <lang.namesValidator
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.refactoring.CrystalNamesValidator"/>

        <notificationGroup id="Crystal Formatting" displayType="BALLOON"/>
        <notificationGroup id="Crystal Rename Verification" displayType="BALLOON"/>
        <notificationGroup id="Crystal Reindex" displayType="BALLOON"/>

        <codeInsight.parameterInfo
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.navigation.CrystalParameterInfoHandler"/>

        <codeInsight.parameterInfo
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.navigation.CrystalAsmParameterInfoHandler"/>

        <stubIndex implementation="io.github.unurgunite.crystal.stubs.CrystalClassIndex"/>
        <stubIndex implementation="io.github.unurgunite.crystal.stubs.CrystalMethodIndex"/>
        <stubIndex implementation="io.github.unurgunite.crystal.stubs.CrystalMethodByClassIndex"/>
        <stubIndex implementation="io.github.unurgunite.crystal.stubs.CrystalClassByEnclosingIndex"/>
        <stubIndex implementation="io.github.unurgunite.crystal.stubs.CrystalMacroIndex"/>

        <stubElementTypeHolder class="io.github.unurgunite.crystal.stubs.CrystalStubElementTypeHolder"/>

        <additionalTextAttributes scheme="Default" file="colorSchemes/CrystalDefault.xml"/>
        <additionalTextAttributes scheme="Darcula" file="colorSchemes/CrystalDarcula.xml"/>

        <newProjectWizard.languageGenerator
                implementation="io.github.unurgunite.crystal.project.CrystalNewProjectWizard"/>

        <directoryProjectGenerator
                implementation="io.github.unurgunite.crystal.project.CrystalDirectoryProjectGenerator"/>

        <projectConfigurable
                parentId="language"
                instance="io.github.unurgunite.crystal.sdk.CrystalSettingsConfigurable"
                id="io.github.unurgunite.crystal.settings"
                displayName="Crystal"/>

        <projectService serviceImplementation="io.github.unurgunite.crystal.sdk.CrystalSettings"/>

        <completion.contributor
                language="Crystal"
                implementationClass="io.github.unurgunite.crystal.completion.CrystalCompletionContributor"/>

        <!-- Icons -->
        <iconProvider
                implementation="io.github.unurgunite.crystal.CrystalIconProvider"/>

        <postStartupActivity
                implementation="io.github.unurgunite.crystal.CrystalSpecSourceRootConfigurator"/>

        <!-- Crystal stdlib as indexed source library -->
        <additionalLibraryRootsProvider
                implementation="io.github.unurgunite.crystal.sdk.CrystalStdlibLibraryProvider"/>

        <!-- Add Crystal stdlib as library source root on project open (triggers indexing) -->
        <postStartupActivity
                implementation="io.github.unurgunite.crystal.sdk.CrystalStdlibSourceRootConfigurator"/>

        <!-- Debugger -->
        <xdebugger.breakpointType
                implementation="io.github.unurgunite.crystal.debugger.CrystalLineBreakpointType"/>

        <programRunner
                implementation="io.github.unurgunite.crystal.debugger.CrystalDebugProgramRunner"/>

        <regExpLanguageHost
                psiElementClass="io.github.unurgunite.crystal.psi.CrystalRegexExpression"
                implementation="io.github.unurgunite.crystal.highlighting.CrystalRegExpLanguageHost"/>
    </extensions>

    <projectListeners>
        <listener
                class="io.github.unurgunite.crystal.refactoring.CrystalRenameVerifier"
                topic="com.intellij.refactoring.listeners.RefactoringEventListener"/>
    </projectListeners>
</idea-plugin>
