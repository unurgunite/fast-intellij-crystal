# Keep GitHub Actions pinned in .github/workflows/* fresh.
# Gradle/Kotlin/IntelliJ updates stay manual — they need compatibility checks,
# not blind bumps.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
